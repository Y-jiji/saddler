Setup statusline to be like:
```
<TOKEN COUNT> @ <FOLDER> (<BRANCH>)
```

Example:
```
1.3k @ ~/Documents/Project (main)
1000.7k @ ~/Documents/Project (master)
```

If not inside `~`, show absolute path. 
If not inside a git repo, show no branch. 

Example: 
```
0k @ /etc/sudoers
```
